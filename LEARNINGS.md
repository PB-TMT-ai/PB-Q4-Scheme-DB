# LEARNINGS.md

| Date | Component | Issue | Resolution | Insight |
|------|-----------|-------|------------|---------|
| 2026-03-20 | Project Setup | Initial scaffold | Created project structure per spec | Follow numbered-section pattern in app.py for maintainability |
| 2026-03-20 | Slab Colors | Tables lacked visual slab distinction | Added color_light field to SLAB_CONFIG; used SLAB_COLORS_LIGHT for row backgrounds in Summary and Dealer Details tables | Always add new SLAB_CONFIG fields to both app.py and scripts/generate_slab_report.py, update blueprint docs and test_helpers required-keys check |
| 2026-03-20 | Dealer Details | Needed bifurcated qualified volumes and next-slab info | Replaced Qualified Volume with Qual. Shop Vol. + Qual. Site Vol.; added Next Slab and Volume to Qualify columns | Use display-level column renaming (not modifying source DataFrame) to present internal column names differently in UI; mirror new derived columns in both app.py and generate_slab_report.py |
| 2026-03-23 | Dashboard V2 | Created minimalist clean redesign of the dashboard | Built `app_v2.py` using Swiss Minimalism design: Inter font, no shadows/gradients, thin 1px borders, generous whitespace, colored dot slab indicators | Keep multiple dashboard versions as separate files (app.py, app_v2.py) sharing the same data pipeline; use `v2_` key prefixes for Streamlit widget state to avoid conflicts |
| 2026-03-23 | V2 CSS Design | V1 used box-shadows, dark gradient header, and zebra-striped tables | V2 uses borderless KPI metrics with blue accent dividers, clean tables with #F9FAFB headers, no zebra striping, and subtle hover states | Minimalist style: remove all decorative elements (shadows, gradients, thick borders); use whitespace and thin lines for visual hierarchy instead |
| 2026-03-23 | V2 Typography | V1 used default sans-serif font | V2 imports Inter from Google Fonts via CSS @import in inject_minimalist_css() | For Streamlit apps, inject Google Fonts via st.markdown with @import url() inside a <style> tag; apply globally with html, body, [class*="css"] selector |
| 2026-03-23 | Deployment | Streamlit Cloud branch dropdown only showed `base` and one other branch | Merged feature branch into `base` via GitHub PR, then deployed from `base` | Streamlit Cloud may not list all branches in the dropdown; deploy from a well-known branch like `base` or `main` rather than feature branches with special characters |
| 2026-03-23 | UI/UX Skills | Used ui-ux-pro-max skill to generate design system | Ran `search.py --design-system` for palette, typography, and style recommendations; used `--domain style/typography/color` for detailed lookups | Always run `--design-system` first for holistic recommendations, then supplement with domain-specific searches for details |
| 2026-03-23 | V2 Bento Grid | Changed V2 from Minimalist Clean to Bento Grid style | Updated CSS to Apple-style: #F5F5F7 page bg, white cards with 20px radius, soft shadows, hover scale(1.02), colored accent bars on slab tiles, pill-style tabs, Fira Sans/Code fonts | Bento Grid key elements: mixed-size cards (2x1 hero + 1x1), rounded corners (20px), .stApp background color, top accent bars for color coding, monospace font for numbers |
